# node_modules is rebuilt inside the image by `npm ci`; copying the host's
# copy in would both bloat the context and risk shipping native modules built
# for the wrong platform.
node_modules
dist
dist-ssr

.env
.env.*
!.env.example

*.local
*.log

.git/
.github/
.vscode/
.idea/
.DS_Store

docs/
*.md
Dockerfile
.dockerignore
